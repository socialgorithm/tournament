{
  "name": "@socialgorithm/model",
  "version": "0.0.16",
  "description": "Data Model for Socialgorithm Tournaments",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && npm run lint",
    "lint": "tslint -p tsconfig.json -t stylish 'src/**/*.ts'",
    "lint:fix": "tslint -p tsconfig.json -t stylish --fix 'src/**/*.ts'",
    "docs": "typedoc --out docs/ --mode modules ./src --theme minimal"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/socialgorithm/model.git"
  },
  "keywords": [
    "socialgorithm"
  ],
  "author": "Socialgorithm",
  "license": "AGPL-3.0-or-later",
  "engines": {
    "node": ">= 8"
  },
  "bugs": {
    "url": "https://github.com/socialgorithm/model/issues"
  },
  "homepage": "https://github.com/socialgorithm/model#readme",
  "devDependencies": {
    "@types/node": "^11.11.0",
    "@types/socket.io": "^2.1.2",
    "@types/socket.io-client": "^1.4.32",
    "pre-commit": "^1.2.2",
    "ts-node": "^8.0.3",
    "tsconfig-paths": "^3.8.0",
    "tslint": "^5.13.1",
    "typedoc": "^0.14.2",
    "typescript": "^3.3.3333"
  },
  "dependencies": {
    "socket.io": "^2.2.0",
    "socket.io-client": "^2.2.0"
  }
}
